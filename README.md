TODO

- Need to only compute loss on masked tokens, not the entire sequence
- Build tokenizer on entire dataset, not just training set. (vocab size?)
- Add positional encoding to the model
- Implement Q, K, V attention
- Add dropout to the model
- Add early stopping
- Test and validate